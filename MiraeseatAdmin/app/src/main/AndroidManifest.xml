<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.capstondesign.miraeadmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/miraeseat_admin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/miraeseat_admin_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".UploadDBActivity"></activity>

        <activity android:name=".HotfixTestActivity"></activity>

        <activity android:name=".inquiry.InquiryActivity"></activity>

        <activity android:name=".inquiry.InquiryContext"></activity>

        <activity android:name=".report.ReportActivity"></activity>

        <activity android:name=".report.ReportContext"></activity>

    </application>

</manifest>